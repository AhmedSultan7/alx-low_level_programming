#include <stdio.h>
/* This is the main function of the program. It prints
   "Hello, world!" to the console and returns 0. */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
